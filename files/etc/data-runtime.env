# Data runtime policy. This file contains no credentials.
# A real /data block mount is preferred. Without it, use the root overlay only
# while this much space remains free; otherwise use tmpfs emergency storage.
DATA_RUNTIME_OVERLAY_MIN_KIB=51200
