# One absolute directory per line. These are additional opt-in backup roots.
# /data/smb is always included and must NOT be repeated here.
# Blank lines and lines beginning with # are ignored. Paths may not contain
# whitespace, glob characters, '..', or point at rclone configuration files.
